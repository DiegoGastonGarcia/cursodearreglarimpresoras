let lista = ['1', '2', '3', '4', '5', '6', '7', '8'];

const = iterationFunction = (element, index)
const findGreaterAndLowerThanFive = (lista) => {
    lista.forEach(element =>)
};

// JSON javascrip object notation

const auto = {
    color: 'gris',
    brand: 'toyota',
    model: {
        year: 2013,
        name: 'corolla',
    }
}

console.log(auto.brand);
console.log(auto.ruedas);